{
  "name": "sample-demo-here",
  "version": "1.0.0",
  "private": true,
  "description": "pnpm and go monorepo with React Router 7 apps and shared UI",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "packageManager": "pnpm@10.24.0"
}
